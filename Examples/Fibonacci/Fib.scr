module Fib;


type <java> "java.lang.Integer" from "rt.jar" as Int;


global protocol Adder(role C, role S)
{
	HELLO(Int, Int) from C to S;

	choice at C
	{
		ADD(Int, Int) from C to S;
		RES(Int) from S to C;
		do Adder(C, S);
	}
	or
	{
		BYE() from C to S;
		BYE() from S to C;
	}	
}